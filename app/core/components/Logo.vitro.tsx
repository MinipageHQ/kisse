import React from 'react'
import { Logo, LogoType, LogoIcon } from './Logo'

export const SimpleLogo = ({ }) => {
  return <Logo />
}
export const SimpleLogoType = ({ }) => {
  return <LogoType />
}
export const SimpleLogoIcon = ({ }) => {
  return <LogoIcon />
}
